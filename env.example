# ============================================================================
# MIMIR CONFIGURATION
# ============================================================================
# Copy this file to .env and customize as needed
# Most settings have sensible defaults - only change what you need

# ============================================================================
# REQUIRED SETTINGS
# ============================================================================

# Neo4j Database Password (CHANGE THIS IN PRODUCTION!)
NEO4J_PASSWORD=password

# Your main source code directory (will be mounted read-only)
# Examples:
#   Windows: C:\Users\YourName\Documents\GitHub
#   macOS:   ~/Documents/projects
#   Linux:   ~/workspace
HOST_WORKSPACE_ROOT=~/src

# ============================================================================
# OPTIONAL: AI EMBEDDINGS (for semantic search)
# ============================================================================
# Enable this for AI-powered semantic search of tasks and files
# Leave disabled if you just want basic task management

# Enable/disable embeddings
MIMIR_EMBEDDINGS_ENABLED=false
MIMIR_FEATURE_VECTOR_EMBEDDINGS=false

# Embeddings Provider (only if enabled above)
# Options: "copilot" (recommended) or "ollama" (local/offline)
MIMIR_EMBEDDINGS_PROVIDER=copilot

# Embedding Model
# For copilot provider:
#   - text-embedding-3-small (recommended - fast and efficient)
#   - text-embedding-3-large (best quality but slower)
# For ollama provider:
#   - nomic-embed-text (good quality, fully local)
#   - nomic-embed-text (higher quality, requires more resources)
MIMIR_EMBEDDINGS_MODEL=text-embedding-3-small
MIMIR_EMBEDDINGS_DIMENSIONS=1536

# Text chunking (advanced - usually don't need to change)
MIMIR_EMBEDDINGS_CHUNK_SIZE=512
MIMIR_EMBEDDINGS_CHUNK_OVERLAP=50

# ============================================================================
# OPTIONAL: ADVANCED FEATURES
# ============================================================================

# LLM API Configuration
MIMIR_LLM_API=http://ollama:11434
MIMIR_LLM_API_PATH=/v1/chat/completions
MIMIR_LLM_API_MODELS_PATH=/v1/models
MIMIR_LLM_API_KEY=dummy-key

# Embeddings API Configuration
MIMIR_EMBEDDINGS_API=http://ollama:11434
MIMIR_EMBEDDINGS_API_PATH=/api/embeddings
MIMIR_EMBEDDINGS_API_MODELS_PATH=/api/tags
MIMIR_EMBEDDINGS_API_KEY=dummy-key

# Auto-index Mimir documentation on startup (default: true)
# This allows users to immediately query Mimir's docs via semantic search
# Set to false to disable automatic documentation indexing
MIMIR_AUTO_INDEX_DOCS=true

# Enable Ecko prompt optimization agent
MIMIR_ENABLE_ECKO=false

# Enable PM model suggestions
MIMIR_FEATURE_PM_MODEL_SUGGESTIONS=false

# ============================================================================
# OPTIONAL: CORPORATE NETWORK SETTINGS
# ============================================================================
# Only needed if you're behind a corporate proxy or use custom certificates

# Proxy settings (uncomment if needed)
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080
# NO_PROXY=localhost,127.0.0.1
# HTTP server port (if using HTTP transport)
PORT=3000

# File watching configuration
FILE_WATCH_POLLING=true
FILE_WATCH_INTERVAL=1000
WORKSPACE_ROOT=/workspace

# Custom CA certificates (uncomment if needed)
# SSL_CERT_FILE=/path/to/corporate-ca.crt
# CURL_CA_BUNDLE=/path/to/corporate-ca.crt
